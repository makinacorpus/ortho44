test was
mount bind the rep in geoserver/data as it need no symlink and plain files and directory
copy all ecw in order with a digit eg 1.ecw é.ecw
generate tindex (shapefile on that)
then add mosaic in geoserver
